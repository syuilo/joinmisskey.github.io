name: Build Pages

on:
  push:
    branches:
    - "src"
    - "pages/**"
    paths-ignore:
    - "theme/**"
    - "!theme/pug/**"
    - "!theme/static/**"
    - "package.json"
    - "yarn.lock"
    - ".config/**"
    - "files/**"

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [13.x]
    env:
      CI: true
      PATREON_BEARER: ${{ secrets.PATREON_BARER }}
      PATREON_CAMPAIGN: ${{ secrets.PATREON_CAMPAIGN }}
    steps:
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - name: Git init
      run: |
        git init
        git remote add origin https://luckybeast:${{ secrets.LB_TOKEN }}@github.com/joinmisskey/joinmisskey.github.io
    - name: Git Next
      run: ./workflows/git-init
    - name: Copy Pages
      run: ./workflows/copy-pages
    - name: Yarn Install
      run: yarn install
    - name: Build
      run: gulp pages
    - name: Git Push Results
      run: ./workflows/git-push-results
